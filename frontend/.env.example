# 车市镜前端环境配置（复制为 .env 后按需修改）

# 后端 API 基址。本地开发指向 scripts/start-dev.ps1 默认启动的 FastAPI 端口。
# 上线时改为线上域名，例如 https://api.cheshijing.com
VITE_API_BASE=http://localhost:8001

# 数据来源开关：
#   mock = 用内置本地 mock（后端未就绪时，按当前 SSE 合同模拟事件流）
#   live = 连真后端（VITE_API_BASE）
# 项目级快速启动默认连真后端；仅做纯前端开发时手动改成 mock。
VITE_DATA_SOURCE=live
